# Temporary override (local-only, optional)
#
# Suggested usage:
# - Keep this file out of git (see .gitignore entry)
# - Use it to scope/limit agent work for a session
#
# Example:
# - Do NOT touch IBKR connectivity code today.
# - Focus only on logging + signal storage changes.
# - Skip replay; run unit tests only.

